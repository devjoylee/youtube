import React from 'react';
import 'styles/app.scss';
import { Header, SideBar } from 'components';

export const App = () => {
  return (
    <div>
      <Header />
      <SideBar />
    </div>
  );
};
